1. 每次功能修改完成，都需要视情况更新CHANGELOG.md，必要时更新README.md文档。
2. 每次全部修改完成后，需要修复linit问题。
3. UI界面问题要确保进行多语言国际化翻译适配（Skill这个是专业词汇，不用进行翻译，始终保持SKill就行）。
4. This is a Tauri desktop app using Rust for the backend and TypeScript for the frontend. When making changes, ensure both layers are updated (e.g., if adding a frontend setting, also add the corresponding backend API/config handler).
